# ExpenseControl Frontend

React + TypeScript app (Vite).

Run:

n```bash
npm install
npm run dev
```

nOpen `http://localhost:3000`. The frontend proxies `/api` to `http://localhost:5000` by default.
